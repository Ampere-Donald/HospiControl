DATABASE_URL="postgresql://user:password@localhost:5432/carnet_medical?schema=public"
JWT_SECRET="replace_with_a_long_random_string"
JWT_EXPIRES_IN="1d"
PORT=4000
# Origine(s) autorisée(s) pour le frontend (CORS). Vide en dev (toutes origines).
# En production : l'URL du front, ex. https://hospicontrol.vercel.app
FRONTEND_URL=""
